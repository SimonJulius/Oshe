import React from 'react';
import styles from './footer.module.scss';

const Footer = () => {
  return (
    <footer className={styles.footer}>
      <div className={styles.contents}>Design by Simon.</div>
    </footer>
  );
};

export default Footer;
